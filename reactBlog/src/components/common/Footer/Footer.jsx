import React from 'react';
import PropTypes from 'prop-types';
//#15171a
const Footer = props => {
    return (
        <div>
            
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;